# Hugging Face / Torch caches (project-local PBS layout)
/.cache/
/.tmp/
/.tools/

# --- Large / local data (not core source) ---
/data/
/checkpoints/
/logs/
/tmp_venv*/
/output/

# OGBench / cube_dataset artifacts (scripts and README live in repo)
cube_dataset/goal_images/
cube_dataset/datasets/
cube_dataset/videos_frames/
cube_dataset/top_episodes/
cube_dataset/gt_export/
cube_dataset/finetune_runs/
# Pi0.5-DROID × OGBench closed-loop rollouts (MP4 + results.jsonl per sample)
cube_dataset/pi05_rollouts/
# Pi0.5-base (UR5e) × OGBench joint-target runs — generated by run_pi05_base_ur5e_rollouts.py + PBS
cube_dataset/pi05_openpi_runs/
# Pi0.5 joint-target rollouts (MP4 covered by *.mp4; keep run_notes.md tracked)
cube_dataset/pi05_joint_rollouts/results.jsonl
cube_dataset/pi05_joint_rollouts/logs/
cube_dataset/*.log

# Massive ML / dataset binaries (regenerate or store outside git)
*.h5
*.hdf5
*.npz
*.npy
*.pth
*.pt
*.ckpt
*.safetensors
*.onnx
*.bin
*.zip
*.tar
*.tar.gz
*.tgz
*.7z
*.mp4
*.webm
*.mov
*.avi
*.mkv
wandb/
lightning_logs/
tensorboard*/
mlruns/
*.pkl
*.pickle
*.joblib
*.arrow
*.parquet

# Nested clones carry their own history (commit inside those repos separately)
# /dino_wm/ and /stable-worldmodel/ already listed below

# Heavy research trees (not VLAW DROID eval pipeline source)
/dino_wm/
/stable-worldmodel/
/old_antigrav/

# External vendored datasets & OpenPI blobs (re-clone / download separately)
/external/LIBERO/
/external/openpi/.git/
/external/openpi/third_party/

# Python / env
__pycache__/
*.py[cod]
*$py.class
.Python
.venv/
venv/
.env
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# OS / editor
.DS_Store
*.swp
*~

# Local debug / stray cluster outputs
strace.out
*.pdf

# PBS stdout/stderr dropped in repo root (7-digit job id suffix)
*.e[0-9][0-9][0-9][0-9][0-9][0-9][0-9]
*.o[0-9][0-9][0-9][0-9][0-9][0-9][0-9]
